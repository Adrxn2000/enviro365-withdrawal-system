# Copy this file to .env and adjust if your backend runs somewhere other
# than the default. Vite only exposes variables prefixed with VITE_ to
# the browser bundle - see src/api/client.js.
VITE_API_BASE_URL=http://localhost:8080
