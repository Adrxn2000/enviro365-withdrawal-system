# Enviro365 Investments — Withdrawal Notice System

Junior Developer Technical Assessment (eTalente / Enviro365, 2026).

- `/backend` — Spring Boot API (see backend/README.md for setup, API docs, business rules)
- `/frontend` — React (Vite) dashboard (see frontend/README.md for setup)

## Quick start
1. `cd backend && mvn spring-boot:run` (starts on :8080)
2. `cd frontend && npm install && npm run dev` (starts on :5173)
